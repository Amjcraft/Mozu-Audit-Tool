<div class="masthead">
  <h3 class="muted">{{seo.default.title}}</h3>
  <div class="navbar">
    <div class="navbar-inner">
      <div class="container">
        <ul class="nav">
          {{#eachNav}}
            <li><a href="{{route}}">{{navtext}}</a></li>
          {{/eachNav}}
        </ul>
      </div>
    </div>
  </div>
</div>